{
  "name": "@loom-loyalty/meridian-cli",
  "version": "0.1.0",
  "description": "Developer CLI for the Meridian protocol. Scaffolds Cloudflare adopter projects, mints admin tokens, runs diagnostics, and talks to the admin surface exposed by @loom-loyalty/meridian-runtime-cloudflare.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/bin.js",
  "bin": {
    "meridian": "dist/bin.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=22"
  }
}
