/**
 * @loom-loyalty/meridian-conformance
 *
 * Shared conformance helpers for Meridian runtimes and component
 * implementations. Exposes validation functions and reference test
 * fixtures so every implementation under test converges on the same
 * shape expectations, while leaving ordering and scoring details
 * implementation-defined.
 */

export * from "./priority/index.js";
