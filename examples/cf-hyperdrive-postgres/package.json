{
  "name": "@meridian/example-cf-hyperdrive-postgres",
  "private": true,
  "version": "0.0.3",
  "description": "Native-Cloudflare Meridian agent: Postgres query-performance monitor via Hyperdrive",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@loom-loyalty/meridian-runtime-cloudflare": "workspace:*",
    "@loom-loyalty/meridian-types": "workspace:*",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.0",
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9",
    "wrangler": "^3.100.0"
  }
}
